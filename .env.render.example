# Environment Variables for Render Production Deployment

# Add these in your Render dashboard under Environment Variables:

# Database (your existing database config)
DATABASE_URL=your_postgresql_database_url_here
DB_HOST=your_database_host
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Application settings
RENDER=true
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# Session secret (generate a secure random string)
SESSION_SECRET_KEY=your_secure_random_secret_key_here

# Portfolio analysis settings (optional - will use defaults)
DEFAULT_RF_RATE=0.02
DEFAULT_DAILY_RF_RATE=0.000055
DEFAULT_SMA_WINDOW=20
DEFAULT_STARTING_CAPITAL=100000

# Upload folder (Render uses ephemeral storage)
UPLOAD_FOLDER=/tmp/uploads

# Node environment for proper React build
NODE_ENV=production
